GRILL-ME TEXT FOR A NEW ENGINE:
Before you create a new engine check _context/infra/ENGINE_AUTHORING.md


GRILL-ME END TEXT:
The end result I want is a carefully detailed plan bronken into steps keeping in mind that each step is going to start from a new session. you're going to link this plan into CURRENT_STATE.md to keep track of it. when asking something use language I can understand, I'm not a programmer, (I know the basics) but don't beat me up with really complex language.



-New engine type External Structured Source:
    A new type of engine that creates a table from External Structured Sources (CSV, Excel, AutoCADDXF, API):
        -Load a DXF file (Can load many files), for each DXF file:
            -Dwg gets added to a list, user can enter date (date default=today), design number, design name, Path
            -Retreaves the blocks name and attributes.
            -User selects block then attributes to be imported
            -User can select more blocks and more attributes
            -Each block is a source in the ETL editor
            -User assing each attribute to the column in the ETL editor
            -Table is created.
            -User can refresh table
                -On each refresh if the program can't find the specified file an error message is returned
                -After a reshresh the program will do an Incremental Sync using the object ID as handle
            -User can change the path of the file

        -Load a CSV, Excel file (Can load many files), for each CSV, Excel file:
            -File gets added to a list, user can enter date (date default=today), design number, design name, Path
            -Retreaves the Sheets name and table Columns.
            -User selects Sheets then Columns to be imported
            -User can select more Sheets and more Columns
            -Each Sheet is a source in the ETL editor
            -User assing each Sheet/Column to the destination column in the ETL editor
            -Table is created.
            -User can refresh table
                -On each refresh if the program can't find the specified file an error message is returned
                -After a reshresh the program will do an Incremental Sync using the TAG as handle
            -User can change the path of the file.


-New engine type Semi Structured Document Source:
    A new type of engine that creates a table from Semi Structured Document Sources (PDF):
        -Load a PDF file (Can load many files), for each PDF file:
            -PDF gets added to a list, user can enter date (date default=today), design number, design name, Path
            -Scan document (Extract Text Objects + Coordinates -> Spatial Grouping -> Pattern Recognition -> Context Association, Nearby texts become metadata)
            -User selects row and column to import
            -Each PDF is a source in the ETL editor
            -Table is created.
            -User can refresh table
                -On each refresh if the program can't find the specified file an error message is returned
                -After a reshresh the program will do an Incremental Sync using the TAG as handle
            -User can change the path of the file


-New engine type Excel Template-driven document generation:
    Design and architect a flexible Excel Template Export System for an engineering/instrumentation web platform.

    The system must allow users to export ETL table data into arbitrary Excel structures/templates while keeping the user as free as possible to design the Excel layout manually.

    IMPORTANT DESIGN PRINCIPLES:
    - Excel owns presentation/layout only
    - The application owns data binding and document generation logic
    - Users should NOT need to write Excel macros/VBA
    - Users should NOT need programming knowledge
    - The export system must support highly variable engineering document structures
    - The export system must scale beyond simple column mapping

    ======================================================================
    CORE CONCEPT
    ======================================================================

    The architecture should separate:

    1. Excel Template
       - Pure XLSX layout/design
       - Formatting
       - Logos
       - Borders
       - Print settings
       - Formulas
       - Merged cells
       - Static labels/text

    2. Export Definition
       - Defines how ETL data binds into the template
       - Defines repeating areas
       - Defines grouping logic
       - Defines sheet creation logic
       - Defines pagination/overflow rules
       - Defines filtering/grouping
       - Defines dynamic region expansion

    3. ETL Data Sources
       - Data already exists as structured ETL tables
       - Export system only consumes those tables

    ======================================================================
    EXAMPLES THAT MUST BE SUPPORTED
    ======================================================================

    ----------------------------------------------------------------------
    EXAMPLE 1 — Instrument Datasheet
    ----------------------------------------------------------------------

    Structure:
    - One Excel sheet describes one instrument
    - Many fixed-position fields
    - Each field contains a single value
    - Example:
      - Tag
      - Service
      - Range
      - Unit
      - Vendor
      - Notes

    Source:
    - ETL table: Instrument_data_sheet

    Requirements:
    - Bind individual cells to ETL fields
    - Example:
      - Cell B4 -> Instrument_data_sheet.tag
      - Cell D8 -> Instrument_data_sheet.service
    - System generates one sheet/document per instrument
    - Must support fixed-position forms

    This should be modeled as:
    - Single Value Binding

    ----------------------------------------------------------------------
    EXAMPLE 2 — Instrument List
    ----------------------------------------------------------------------

    Structure:
    - Infinite/repeating table
    - Each row = one instrument
    - Each column = one field
    - Dynamic row expansion

    Source:
    - ETL table: Instrument_list

    Requirements:
    - User selects a template row
    - System repeats row dynamically for each record
    - Example:
      - A12 -> tag
      - B12 -> service
      - C12 -> unit
    - Template row becomes a repeating row template
    - Row insertion/expansion handled by export engine
    - Formatting must propagate correctly

    This should be modeled as:
    - Repeating Table Binding

    ----------------------------------------------------------------------
    EXAMPLE 3 — Assembly Type Sheets
    ----------------------------------------------------------------------

    Structure:
    - One sheet per assembly type
    - Sheet contains:
      1. Small materials table
      2. List of instruments using that assembly type

    Sources:
    - Assembly_type_standard
    - Instruments_Assembly_type

    Requirements:
    - Group data by assembly_type
    - Create one sheet per assembly type
    - Render repeating material table
    - Render repeating instrument list
    - Maximum number of instruments per sheet is limited by a defined Excel range
    - If exceeded:
      - Automatically create continuation sheet(s)

    Must support:
    - Grouping
    - Sheet-per-group generation
    - Multiple repeating regions in same sheet
    - Overflow/pagination rules

    ======================================================================
    UI/UX REQUIREMENTS
    ======================================================================

    The system should NOT behave like a simple CSV mapper.

    The UI should behave like a:
    - Template Binding Designer
    - Document Generation Designer

    The AI should design an interface with these concepts:

    ----------------------------------------------------------------------
    1. Excel Template Visual Preview
    ----------------------------------------------------------------------

    - Render uploaded Excel template visually
    - Allow clicking cells/regions
    - Show sheet structure
    - Allow selecting:
      - single cells
      - row templates
      - rectangular regions

    ----------------------------------------------------------------------
    2. Data Source Browser
    ----------------------------------------------------------------------

    Side panel showing ETL sources:

    Example:
    Instrument_list
     ├── tag
     ├── service
     ├── range
     ├── unit

    Users drag/drop or bind fields visually.

    ----------------------------------------------------------------------
    3. Binding Types
    ----------------------------------------------------------------------

    The interface must explicitly support multiple binding modes:

    A. Single Value Binding
    - Bind one field to one cell

    B. Repeating Table Binding
    - Bind row template to dataset
    - Repeat dynamically

    C. Grouped Section Binding
    - Create grouped sheets/documents
    - Multiple repeating regions per sheet

    D. Overflow/Pagination Rules
    - Create continuation sheets
    - Define maximum rows per region
    - Repeat headers automatically

    ----------------------------------------------------------------------
    4. Named Regions
    ----------------------------------------------------------------------

    The system should strongly consider named regions instead of relying only on coordinates.

    Examples:
    - MATERIAL_TABLE
    - INSTRUMENT_TABLE
    - HEADER_REGION

    Regions should carry behaviors:
    - repeating
    - grouped
    - paginated
    - fixed

    ======================================================================
    ARCHITECTURAL REQUIREMENTS
    ======================================================================

    The AI should propose:

    - Internal export definition schema
    - Data binding model
    - Runtime rendering engine
    - Template parsing architecture
    - Region abstraction system
    - Dynamic row insertion strategy
    - Pagination strategy
    - Sheet cloning strategy
    - Group-by rendering architecture
    - Validation/error handling
    - Mapping persistence
    - Future extensibility

    ======================================================================
    IMPORTANT CONSTRAINTS
    ======================================================================

    - Users must remain free to design arbitrary Excel layouts
    - Export engine must support engineering documents, not only flat reports
    - Must support:
      - merged cells
      - print layouts
      - formulas
      - logos/images
      - multi-sheet workbooks
      - continuation sheets
      - grouped documents
    - The system must NOT depend on VBA/macros
    - Logic belongs in the application, not inside Excel

    ======================================================================
    GOAL
    ======================================================================

    The final system should resemble a lightweight engineering-oriented document generation engine specialized for instrumentation and industrial documentation.

    The AI should focus heavily on:
    - abstraction design
    - UX
    - export architecture
    - scaling complexity
    - maintainability
    - future extensibility
    - avoiding tight coupling between templates and ETL sources






Help me write a raw prompt for claude code, don't suggest any code yet, let claude code do it, I just need a good, understandable description of this:

"
This is big, so I think you should divide this in more grill-me sessions.
Read _context/infra/ENGINE_AUTHORING.md for reference
We're making a new engine. its purpose is a typical assembly and Material Take Off:
-we're going to use the same grid and the ETL sheet v1 uses
-it has to be multipurpose (not just for instrumentation)
-it will consist in many typical assembly (normally a page for typical), each one with:
    -a name.
    -a description of the tipical
    -an image (originally a DWG, but if is not possible PDF and DXF, prefer scalable formats that don't loose resolution, but I guess it has to support also JPEG, PNG and other comon formats too)
    -a material table (created from ETL)
    -a utilities table (created from ETL) utilities assing to that typical
    -the user needs to "link" each material in the desing, we need to discuss this but basically the user drags a row from the table to the desing, it shows the TAG (since is a system column that already exist) inside a circle or rectangle with an arrow pointing where the user decides to point inside the desing, this is saved in the project database. the end result has to be able to export this desing in excel with the those tags in the right position.
    -it has a panel that enables the user to import other typicals from an external database (another project database or another library database that contains typicals) the panel has to show the preview of each typical as the user imports it, suggest me how to adress difrent tables structures. but basically the material ussually have a TAG, REV and LOG as system columns and part description, size, material, unit of meassurement, quantity, and total (not sure if total can be calculated in the ETL, "quantity"*"the number of rows in the utilities table".

here are some real world scenarios as an example:
    -User creates a new typical sheet engine.
    -opens the typical sheet engine, is empty.
    -utilities table: uses the ETL to load the utilities assinged to each typical: TT01: TYPICAL01, FT03: TIPICAL02, LT02: TYPICAL01, AT02: TYPICAL03, ect, from the project database. User decides how many columns to make, that's not important the Engine cares only for the TAG and how many rows it has.
        -the utilities table will have same functionality as the grid in "sheet_v1", FLAGS, LOGS, REV, be able to manual edit, ad new rows, ect.
    -the engine shows one page for each unique typical. user can select the page from a dropbox with the tipicals name (TYPICAL01, TIPICAL02, TYPICAL03) in the "tool-toolbar", each page has its own utilities table for that especific typical (filtered).
    -material table: the user writes the ETL (or loads a template) that will import all parts required for each typical from the project database or just creates an empty table if there's none.
        -there should be a material catalog, where the materials TAG (all system comlumns, REV and LOG), part description, size and UOM are stored.
        -the engine database (material table) will import a material part description, size and UOM from the catalog when the user writes adds a new line with a TAG.
        -the table will suggest entries from the catalog while the user writes in the cell.
        -if the user adds a new line that the catalog doesn't have this material will be added to te catalog.
        -if the user edits a material with the same TAG this material will be updated to the catalog.
        -if the user changes a tag in the material table, the values will be re-imported from the catalog.
        -catalog and material table have to be synched between each other.
        -The material table will save in the database all values inserted or imported from the catalog plus the name, description, quantity of each material and the note.
    **the angine already has created some tables, see how to fit this description into them, or lets discuss it so you can suggest a better approach**
    -Since there are 2 tables (material and utilities) the there will be 2 ETL working simultaniusly.
    -Fisrt page: TYPICAL01 is new, empty (no materials, no image, just the utilities assinged to TYPICAL01)
        -user loads image
        -fills the material list: assing TAG, writes part description, size, material, unit of meassurement and quantity the total is calculated multiplying the quantity times the number of rows in utitlities (probably from the ETL).
        -drags each row from the material list to the image, shows the TAG of that row as a preview until user release the mouse button, after the TAG is placed, the user then clicks again to position the arrow pointing somewhere in the image. tag and arrow can be move afterwards
    -Second page: TYPICAL02 already has its material list imported from the project database.
    -Third page: TYPICAL03 is empty (no materials, no image, just the utilities assinged to TYPICAL01)
        -user uses the import typical panel
        -clicks on "import from database"
        -selects the database to import from (can be another project or a catalog database both made from this engine they will have the same structure so it won't matter)
        -the panel shows a list of typicals (made with the same engine and compatible version)
        -the user selects TYPICAL03
        -import
        -the panel shows it has imported TYPICAL03 successfully
    -If the user adds a new utiliti with a new typical
        -refresh the ETL
        -Engine shows a new page with the new typical (emmty or not)
    -The user export this typical into another project database used as a catalog.
        -clicks export typical
        -Selects the typical/s to export
        -Engine checks if the typical name already exist: if yes - ask user for a new name / if no - saves the typical

The end result I want is a carefully detailed plan bronken into steps keeping in mind that each step is going to
start from a new session. you're going to link this plan into CURRENT_STATE.md to keep track of it. when asking
something use language I can understand, I'm not a programmer, (I know the basics) but don't beat me up with really
complex language.
"