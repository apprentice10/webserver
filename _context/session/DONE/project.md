# session/DONE/project.md

*Completed project management features — append one bullet per closed task.*

---

* Project management (create/open/delete, sessionStorage)
* **Architectural refactor — plugin system + single project DB** (2026-04-26): removed SQLAlchemy; `registry.db` replaced by `data/projects.db` (raw sqlite3, `engine/project_index.py`); `_project` + `_templates` inside each project DB; `engine/catalog.py` dynamically scans `tools/*/tool.json`; `tools/instrument_list/tool.json` created; removed `database.py`, `core/models.py`, `core/audit.py`, `engine/models.py`
* **Export Excel** (2026-05-01): `GET /api/tools/{tid}/export/excel` — openpyxl, blue bold header, freeze row 1, auto width; "Export Excel" button calls `window.location.href`
* **Group I tasks 47–48 — Project export/import** (2026-05-07): `GET /api/projects/{id}/export` returns `.db` as FileResponse; `POST /api/projects/import` accepts `.db` upload, validates SQLite magic bytes + `_project` table, copies to `data/`, registers in index; export (⬇) button + "📂 Import from file" button in Open Project modal
* **Stateless server + local file management** (D-S1 through D-S8, 2026-05-08): removed `data/projects.db` + `engine/project_index.py`; server is pure file-operation layer — project identity is full `.db` path as `?db=...`; `get_project_conn` reads `?db=` directly; `core/routes.py` rewritten: `POST /api/project/new`, `POST /api/project/open`, `GET/DELETE /api/project`, `POST /api/project/backup`, `GET /api/fs/browse`, `GET /api/fs/cwd`; `main.js` rewritten with localStorage recents + filesystem browser IIFE; all JS modules migrated from `PROJECT_ID` to `DB_PATH`; settings modal gains Backup tab
* **Group J — Schema versioning + compatibility** (2026-05-08): `PRAGMA user_version` stores schema version; `SCHEMA_VERSION = 1` constant in `engine/project_db.py`; `_run_migrations` versioned runner (one transaction per version step with rollback on failure); all pre-existing checks absorbed into `_migrate_to_v1`; safety backup to `data/backups/<stem>_pre_migration_v<N>.db`; HTTP 403 on writes from newer-DB-than-server; `schema_warning` in `GET /api/project` metadata; D12 in `infra/DECISIONS.md`
* **Group I task 49 — Automatic backup** (2026-05-08): on-open toggle + timer interval in Settings → Backup tab
* **Group Q — Q0: Schema + Migration** (2026-05-15): `SCHEMA_VERSION` bumped to 2; `_revisions` + `_revision_snapshots` tables added to `DDL_SYSTEM_TABLES`; `rev` system column changed to `integer` (default 0) in `SYSTEM_COLUMN_DEFS` and `create_tool_table`; `_migrate_to_v2` seeds rev-0 and resets TEXT `rev` values to 0; `create_project_db` seeds revision 0 ("First issue") for new projects
* **Group Q — Q1: Backend Revision CRUD Endpoints** (2026-05-15): `engine/routes_revisions.py` created — `GET /api/project/revisions`, `POST /api/project/revision`, `DELETE /api/project/revision/{number}`, `POST /api/project/revision/{number}/revert`; router registered in `main.py`; safety backup on revert; snapshot captured under current revision id at create time
* **Group Q — Q3: Backend Snapshot Read Endpoint** (2026-05-15): `GET /api/project/revision/{number}/tool/{tool_slug}` added to `routes_revisions.py`; returns `{ columns, rows }` parsed from `_revision_snapshots`; 404 on unknown revision or missing snapshot; companion `.md` updated with new line range
* **Group Q — Q2: Auto-update REV on Mutations** (2026-05-15): `get_current_revision(conn)` added to `project_db.py` as the sole source for active revision number; `audit()` `revision` param changed from `str` to `int`; `update_cell`, `create_row`, `restore_row`, `paste_rows`, `rollback_cell` all use `get_current_revision` instead of `tool["rev"]`; `update_cell` and `rollback_cell` stamp `rev` on the mutated row in the same UPDATE; `etl_apply` stamps `rev` on every modified/inserted row and passes `revision=rev` to all audit calls
* **Group Q — Q4: Frontend chip-rev Button + Picker** (2026-05-15): `RevisionPicker` IIFE created at `static/engine/js/revision-picker/revision-picker.js`; owns `#chip-rev-btn` text (`Rev N`); fixed-position popover lists all revisions (newest first) with latest badge and delete button; inline create form (description + author) → `POST /api/project/revision`; delete with confirm → `DELETE /api/project/revision/{number}`; `ApiClient` extended with `getRevisions`, `createRevision`, `deleteRevision`; `toolbar.js._updateToolUI` no longer sets chip text; `table.html` loads the new script and calls `RevisionPicker.init()`
* **MTO Engine — Typical Assembly and Material Take-Off** (2026-05-17, Steps 1–22): Full `mto_v1` plugin — 5 DB tables, CRUD for tool/typicals/materials/images/placements, ETL integration (`mto_utilities` target, auto-page creation), multi-page tab UI, image upload (SVG/DXF/PDF), drag-drop TAG annotation with arrow lines, import from external DB, export to external DB with conflict resolution, Excel export (openpyxl, per-typical worksheets, SVG→PNG via cairosvg when libcairo available, tag label text boxes injected via zipfile surgery into drawing XML at stored label_x/label_y coordinates)

* **Phase 5 — Drawing Toolkit** (2026-05-19, Steps 1–13): `_images` + `_annotations` system tables (SCHEMA_VERSION 8); `dashboard/images.py` + `routes_images.py` (upload/list/blob/delete/replace); `dashboard/annotations.py` + `routes_annotations.py` (list/create/update/delete, partial update for drag); `static/engine/js/toolkits/drawing/drawing.js` IIFE — image gallery, SVG annotation overlay (pin/arrow/rectangle/text), zoom/pan via CSS transform, pdf.js ESM lazy-load + page nav, tool mode toolbar, drag-to-move (pointer capture + SVG translate), cross-toolkit events (`drawing:annotationSelected` ↔ `grid:rowSelected`), style presets persisted to `toolkit_config`; D38 (shared backend services in dashboard/), D39 (TAG string as row-link key); registered in `engines/sheet_v1/engine.json`

* **Group Q — Q5: Frontend Read-only Mode + Revert Flow** (2026-05-15): Clicking an old revision in the picker loads its snapshot via `GET /api/project/revision/{N}/tool/{slug}` → `GridManager.loadSnapshotData` replaces columns + rows; amber `#readonly-banner` shown with "Back to current" + "⚠ Revert" buttons; `data-readonly` on `#tool-container` hides ghost row + dims grid via CSS; `CellSave.doSaveCell` and `_createFromGhost` no-op when `getViewingRevision() !== null`; `ContextMenu.open` hides all mutation items in read-only mode; ETL run button disabled + ETL editor link dimmed; `revision:switched` DOM event resets history panel `_logLastKey`; all three `getAudit` calls in `table.html` pass `revision: RevisionPicker.getViewingRevision()` for scoped history; backend `/audit` endpoint gains optional `?revision=` filter; `_backToCurrent` calls `GridManager.reloadData()` (not `init()`) to avoid duplicate event listeners; revert flow: confirm dialog → `POST /api/project/revision/{N}/revert` → `location.reload()`; `ApiClient` extended with `getRevisionSnapshot`, `revertRevision`; `ToolbarManager.getToolSlug()` added; `ColumnsManager.loadFromData()` added
