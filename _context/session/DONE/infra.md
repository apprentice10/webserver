# session/DONE/infra.md

*Completed infrastructure / refactoring tasks — append one bullet per closed task.*

---

* **Phase 3 — service.py / routes.py decomposition** (2026-05-14): 7 tasks. `schemas.py` (147 LOC), `service_templates.py` (51 LOC), `service_columns.py` (249 LOC), `service_row_ops.py` (310 LOC), `routes_flags.py` (133 LOC), `routes_etl.py` (107 LOC), `routes_export.py` (77 LOC). Final: `service.py` 914→345 LOC, `routes.py` 840→402 LOC. 61/61 tests pass.
* **Phase 4 — history subsystem extraction from grid.js** (2026-05-14): 6 tasks (P4-H1–H6). `history/history-api.js` (13 LOC), `history/history-renderer.js` (51 LOC), `history/history-panel.js` (150 LOC), `rollback-service.js` (27 LOC), `history-actions.js` (19 LOC). Grid.js: `grid:rowUpdated` listener added, history functions removed (~270 LOC). 61/61 tests pass. Feature-subdirectory convention (P4-D3) established.
* **Phase 4 — etl_editor.js decomposition** (2026-05-14): 4 tasks (P4-E1–E4). `etl-expr.js` (235 LOC), `etl-model-renderer.js` (307 LOC), `etl-preview-renderer.js` (56 LOC), `etl-persistence.js` (200 LOC). `etl_editor.js` reduced 1174→480 LOC. 61/61 tests pass.
* **Phase 4 — grid.js decomposition** (2026-05-14): 7 tasks (P4-G1–G7). `selection/selection-manager.js` (282 LOC), `keyboard/cell-keyboard.js` (226 LOC), `context-menu/context-menu.js` (286 LOC), `rendering/grid-renderer.js` (128 LOC), `row-ops/row-ops.js` (139 LOC), `clipboard/clipboard-manager.js` (63 LOC), `cell-save/cell-save.js` (72 LOC). `grid.js` reduced 1670→~465 LOC. 61/61 tests pass.
* **Phase 4 — panel_system.js decomposition** (2026-05-14): 2 tasks (P4-P1+P4-P2). `panels/panel-floats.js` (192 LOC), `panels/panel-tab-bar.js` (145 LOC). `panel_system.js` reduced 634→356 LOC. Phase 4 complete.
* **Architecture audit + cleanup** (2026-05-08): deleted orphaned `static/js/main (1).js`; fixed `etl_canvas.js:349` (`_projectId` undefined after D-S1); removed dead `showToast` alias from `etl.html`; removed unused `project_id` from Pydantic models; renamed `row_id` → `trash_id` in `restore_row`; updated `etl_canvas_editor.js.md`.
* **Phase 1 — ETL compiler decomposition** (2026-05-14): `etl_compiler_expr.py` (124 LOC), `etl_compiler_graph.py` (87 LOC), `etl_compiler_validate.py` (230 LOC). `etl_compiler.py` reduced 727→~20 LOC (orchestration only). `sql_to_model_expr.py` (295 LOC), `sql_to_model_lexer.py` (123 LOC). `sql_to_model.py` reduced 895→388 LOC. 61/61 tests pass.
* **Phase 2 — Bug isolation** (2026-05-14): diagnostic logging added to `project_db.py` + `routes.py`; root cause of intermittent 500 (missing `__position` index) identified and fixed by Item 43; Phase 2 closed without repro.
* **_context restructure** (2026-05-15): migrated flat `_context/` to categorized subfolders (`etl/`, `grid/`, `project/`, `infra/`, `session/DONE/`); RISKS.md eliminated (risks live in plan files); CURRENT_STATE.md rewritten as pure index; CLAUDE.md rewritten as behavioral-rules-only; MODULE_LAYOUT.md created in `infra/`; all completed plan files deleted.
* **Engine Management UX** (2026-05-15): Steps 1–5. Duplicate-name check on engine creation (real-time, case-insensitive); canvas as default ETL view; engine drag-to-reorder (`_tools.position`, schema v5, `PATCH /{tool_id}/position`); engine groups (`_tool_groups` table, schema v6, 4 group endpoints, collapsible sidebar sections); soft-delete to trash + restore + permanent delete (`is_trashed`/`trashed_at` on `_tools`, schema v7). Cross-cutting decisions: D-EM01–D-EM10.
* **Group R6 — Self-Contained Engine Folders: Sheet V1** (2026-05-15): 7 steps (R6-1–R6-7). Renamed `engine/` → `dashboard/`; added dynamic static mount + route loader in `main.py`; moved Sheet V1 backend (routes, service, schemas) → `engines/sheet_v1/backend/`; moved Sheet V1 JS/CSS → `engines/sheet_v1/static/`; moved `table.html` → `engines/sheet_v1/templates/`; created 11 companion `.md` files; updated MODULE_LAYOUT.md with new paths; added D15 (self-contained engine layout) to DECISIONS.md.
* **Sidebar Groups UX** (2026-05-15): 7 steps (0–6). Fixed `POST /api/project/open` missing `is_trashed` filter + added `group_id` to open-project response; pinned `+New Engine`/`+Group` buttons at bottom of sidebar via flex layout on `.side-section`/`.side-nav`; trash rendered as collapsible `.side-group` (collapsed by default, `_trashCollapsed` JS state); edit pencil on engine sidebar items calls `AppShell.openToolPopover`; `prompt()` dialogs replaced with `SidebarGroups.openGroupPopover` popover + edit pencil on group headers; groups draggable (reorder via PATCH `groups/{id}`); engine drag-to-group-header assigns group, drag-to-ungrouped-zone clears `group_id`. New file: `static/js/sidebar_groups.js`.
* **Group R — UI Polish: columns, panels, density** (2026-05-15): 10 steps. Removed LOG/REV from Panels dropdown (Step 1); system columns now draggable frontend-only, API not called for system col reorder (Step 2); per-column Hide/Show hidden in column right-click context menu (Steps 3–5); column order + hidden state persisted to `PanelSystem.extra` (same localStorage key) and restored on load (Step 6); `showPanel` fixed to activate existing dock location before using caller hint (Step 7); density control replaced with 9–16px slider in settings modal (Step 8); `setDensity` rewired to numeric CSS vars lookup table (Step 9); legacy `dense`/`comfortable` string prefs migrated (Step 10). Cross-cutting decisions: D17 (PanelSystem.extra for column state), D18 (CSS injection for column hiding), D19 (numeric density).
* **Phase 1 — Toolkit System Core** (2026-05-19): 9 steps. `_toolkit_config` table added to project DB (schema v12, `_migrate_to_v12`); `GET /api/engines/{slug}/tools/{tool_id}/toolkit-config` endpoint in `dashboard/routes_toolkit.py`; `ToolkitHost` IIFE written (`static/engine/js/toolkit_host.js`) with async init, event bus, four-bucket state, getToolkit; `engine.json` format updated in both `sheet_v1` and `mto_v1` (replaces `dashboard_uses` with `toolkits: []`); Jinja2 templates (`table.html`, `mto_table.html`) inject `window.__ENGINE_CONFIG__`, load `toolkit_host.js`, and call `ToolkitHost.init()` at DOMContentLoaded; `main.py` passes `engine_slug` + `toolkits` to page routes; `ENGINE_AUTHORING.md` updated with toolkit contract + Host API surface; companion `.md` written. Cross-cutting decisions: D20–D30.
