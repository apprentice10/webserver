# DONE.md

*Feature log — completed and verified in production.*

---

* Project management (create/open/delete, sessionStorage)
* Universal Table Engine: inline cell edit, keyboard nav (Tab/Enter/Arrows)
* Ghost row at grid bottom for fast row insertion
* Paste from Excel/CSV (range paste + append paste)
* Soft delete (strikethrough, restorable) + hard delete (trash only)
* Per-row LOG column with full change history (rev, timestamp, old→new)
* Column resize + reorder by drag (persisted to DB)
* Settings panel: tool name, icon picker, revision
* ETL Editor (`/tool/{pid}/{tid}/etl`): SQL → Preview → Apply
* ETL: auto-creates columns, merges by TAG, respects `is_overridden` cells
* ETL version history, template save/load (scoped by type_slug + project_id)
* Power SQL Editor (arbitrary SELECT/DML, no DDL)
* Right-click context menu (delete, restore, hard-delete, log)
* Toggle LOG column visibility (CSS class, no re-render)
* ETL staleness tracking: `is_stale` flag, orange badge in sidebar
* ETL dependency graph: `etl_deps` extracted from SQL at save time
* Topological ETL auto-run (`etl_run_saved` with `_visited` cycle guard)
* Circular dependency detection (HTTP 400 on cycle)
* Tool creation with ETL SQL from file + template scoping by tool type
* Schema browser in ETL editor
* Toolbar: stale badge, run ETL button, settings access
* Horizontal scrollbar for wide tables (`grid-scroll-container` + `width: max-content`)
* Toggle REV column visibility (CSS `.rev-hidden` class, same pattern as LOG toggle)
* Toolbar toggle buttons unified: icon + fixed label, `.active` CSS indicates state; deleted button adds 🗑 icon
* Double-click on `.resize-handle` → auto-fit column width to the widest content (header + visible cells), saves to backend
* Single click = cell selection (select mode, readonly + light outline), double click / Enter / char = edit mode; Escape cancels; blur saves and returns to select mode
* Fix: drag-and-drop column reorder had no visual effect — `_columns.map` iterated in original order and assigned positions in the wrong order; fixed with `userCols.forEach` that mutates positions in-place on shared references, then `_columns.sort`
* Row numbers: Excel-style index column (fixed on the left, background/text like header, not editable, does not participate in keyboard nav); 1-based number based on `_filteredRows` index; empty cell in the ghost row
* Context menu "Remove manual override": `DELETE /{tid}/rows/{rid}/override?col=X` restores `etl_value` in the cell and deletes the row from `_overrides`; item visible only on right-click on a cell with triangle
* Triangle `is_overridden` + ETL tooltip: `_overrides` adds column `etl_value TEXT`; automatic migration in `_migrate_project_db`; `INSERT OR IGNORE` preserves the original ETL value on first override; `overridden_cols` is now `dict[col_slug → etl_value]`; frontend shows `title="ETL Value: ..."` on the overridden `<td>`
* Export Excel: route `GET /api/tools/{tid}/export/excel?project_id=N` — openpyxl, blue bold header, freeze row 1, auto width, excludes LOG column; "Export Excel" button in toolbar calls `window.location.href` for direct download
* **Architectural refactor — plugin system + single project DB** (2026-04-26): completely removed SQLAlchemy; `registry.db` replaced by `data/projects.db` (raw sqlite3, `engine/project_index.py`); project metadata (`_project`) and ETL templates (`_templates`) now inside each project DB; `engine/catalog.py` now dynamically scans `tools/*/tool.json`; `tools/instrument_list/tool.json` created as plugin manifest; removed `database.py`, `core/models.py`, `core/audit.py`, `engine/models.py`
* **Right-click inside range = keep selection + range context menu; right-click outside range = new selection + single-cell context menu** (tasks 16–17): `_handleContextMenu()` in `grid.js` checks whether the right-clicked cell is inside the current `_ranges`; if yes, the selection is preserved and the range context menu is shown; if no, the range is cleared, the clicked cell becomes the new single-cell selection, and the standard single-cell context menu is shown
* **Range selection + Shift+click + Ctrl+click + column/row header click** (tasks 10–14): click or click+drag on cells selects a rectangular range; `.cell-selected` CSS highlight; `data-row-idx`/`data-col-idx` attrs on each `<td>`; `_rangeStart`/`_rangeEnd` track the anchor and drag end; `_updateRangeHighlight()` applies classes; Escape clears selection; cleared on `render()` and `_enterEditMode()`; `getRange()` / `clearRange()` exposed publicly for Shift+click (task 11) and Ctrl+C (task 15)
* **Ctrl+C range copy to clipboard** (task 15): `_initCopyToClipboard()` in `grid.js` intercepts `Ctrl+C`/`Cmd+C` when a range is selected and no cell is in edit mode; computes bounding box across all `_ranges`; builds tab-separated rows joined by `\n`; writes to clipboard via `navigator.clipboard.writeText()`; shows success toast with cell count; non-selected cells in the bounding box (discontinuous Ctrl+click selections) are copied as empty strings
* **Sidebar shell** (task 18): collapsible right-side panel (`#sidebar-panel`) toggled by toolbar button "▶ Info"; `SidebarManager` IIFE in `sidebar.js` exposes `toggle/open/close/isOpen/setTitle/setContent/clearContent`; open/close uses CSS class `sidebar-closed` on `.sidebar-panel` (180ms width transition); layout restructured — `.tool-body` flex-row wraps `.tool-main` (grid+note+sql) and `.sidebar-panel`; starts closed; ready for LOG and FLAG sections (items 19–20)
* **Sidebar: single cell LOG** (task 19): right-click on any data cell shows "Vedi LOG cella" in context menu; `grid.js::showCellLog(rowId, colSlug)` filters `row.row_log` entries by column slug (pattern `] COL_KEY:`), renders a meta header (column + row tag) and a list of timestamped change entries in the sidebar; CSS classes in `sidebar.css` (`.sidebar-log-*`); `_ctxColSlugLog` state variable captures the clicked column regardless of override status
* **Sidebar: range LOG** (task 20): right-click on a multi-cell selection shows "View selection LOG" in context menu (replacing "View cell LOG" which shows only for single-cell); `grid.js::showRangeLog()` iterates `_ranges`, groups log entries by column then by row tag, renders a collapsible tree in the sidebar; `_isSingleCellSelection()` helper determines which menu item to display; new CSS classes `.sidebar-log-group`, `.sidebar-log-group-header`, `.sidebar-log-row-label` in `sidebar.css`
* **Flag DB schema** (task 25): `_flags(id, name, color, is_system)` + `_cell_flags(tool_slug, row_tag, col_slug, flag_id)` added to `DDL_SYSTEM_TABLES` and migrated in `_migrate_project_db`; system flags "manual_edit" (`#FF8C00`) and "ETL: Eliminated" (`#DC143C`) seeded via `INSERT OR IGNORE`; `col_slug=''` used as sentinel for row-level flags to avoid NULL in composite PK
* **Multi-flag visual indicator** (task 27): `get_rows()` in `service.py` bulk-loads `_cell_flags` JOIN `_flags` and attaches `cell_flags: {col_slug: [{id,name,color}]}` to each row (`col_slug=''` = row-level); `_renderCell()` in `grid.js` injects `.cell-flag-badges` span with `.cell-flag-dot` circles (colored, titled) inside the `<td>`; `td[data-has-flags="true"]` gets `position:relative`; row-level flags rendered next to the row number via `.row-num-flags`; stacking supported (flex row of dots)
* **Flag management sidebar** (task 26): `FlagsManager` IIFE in `flags.js`; 4 backend routes under `/api/tools/flags[/{id}]?project_id=N` (GET list, POST create, PATCH update, DELETE); system flags: color-only editable, rename/delete blocked at API level; "⚑ Flags" button in toolbar opens sidebar panel; inline color picker + name input per row; add-form at bottom; CSS in `sidebar.css` (`.sidebar-flag-*`)
* **Cumulative flag tooltip + FLAG context menu** (tasks 28–29): each `.cell-flag-dot` circle carries an individual `title` tooltip; `manual_edit` dot shows `"ETL: {etl_value}"` or `"ETL: (empty)"`; context menu gains a "Flag ▶" submenu listing all project flags with checkmarks for flags already applied to the target cell/row; clicking a flag entry toggles it via `POST/DELETE /api/tools/flags/cell?project_id=N`
* **SQL syntax highlighting + auto-format** (Group F, tasks 30–31): ETL editor (`etl.html`) integrates CodeMirror 5 via CDN; `_cmEditor` replaces the plain textarea; SQL mode with line numbers, `Ctrl+Enter` → preview, Tab → indent; `formatSql()` uses `sql-formatter` v15 via CDN (UMD global), adds "⌥ Format" button to the actions bar; graceful fallback to plain textarea if CDN scripts fail to load
* **ETL orphan row management** (Group F.1, tasks 33–36): `etl_apply` computes orphan tags (existing rows not in ETL result) and inserts `ETL: Eliminated` row-level flag (`col_slug=''`) for each; removes the flag for rows that reappear in ETL source; audited as `ETL_ELIMINATED`; return dict includes `orphaned` count; route `POST /{tid}/rows/{rid}/keep?project_id=N` removes the flag and audits as `KEEP_ROW`; context menu entry "✓ Keep row" visible only on eliminated rows; `row-eliminated` CSS class applies crimson-tinted background + italic muted text to the entire row
* **Group H tasks 44–45 — Bidirectional ETL foundation** (2026-05-01): feasibility analysis in `_context/ETL_BIDIRECTIONAL.md`; `delete_column` now detects ETL-generated columns via `lineage_info` and removes the corresponding SELECT expression from the saved SQL atomically; `sql_parser.remove_col_from_sql` added with depth-0 split + alias matching; raises HTTP 400 if last column; `columns.js` shows ETL-aware confirm dialog and "query ETL aggiornata" toast; staleness helpers extracted to `engine/staleness.py` (RISKS.md R01 partial split)
* **Task 45b — Bidirectional ETL: rename column + bug fix** (2026-05-01): fix: `etl_apply` now persists the applied SQL as a draft to `query_config.etl_sql` (read-modify-write before `conn.commit()`), which was the root cause of delete not updating the SQL; new `sql_parser.rename_col_in_sql(sql, old, new)` appends or replaces `AS alias` on the matched item; `update_column` detects ETL columns via `lineage_info`, computes `new_slug = slugify(new_name)`, renames the DB column (`ALTER TABLE RENAME COLUMN`), updates `_overrides.col_slug`, updates `query_config.etl_sql`; `columns.js` shows confirm dialog and "ETL aggiornata" toast for ETL column rename
* **Model-first ETL — backend layer** (2026-05-02): new `engine/etl_model.py` (dataclasses: `EtlModel`, all transformation types, expression union); new `engine/etl_compiler.py` (`compile_sql`, `validate_model`, `expr_to_sql`, `_kahn_sort`); `engine/etl.py` rewritten — all functions accept `model: dict`, SQL is compiled from model, deps derived from `model.sources`, lineage from model structure (no `sql_parser` at runtime); `query_config` now stores `{etl_model, etl_sql, etl_deps, etl_history}` where `etl_sql` is compiled output for display only; history entries store `{model, sql, label, timestamp}`; 9 tests in `tests/test_etl_model.py` all green; steps 5–8 (routes.py, service.py, api.js, etl_editor.js) still pending — see CURRENT_STATE.md
* **Group G — LOG as revision/audit system** (tasks 37–41): `_audit` table extended with `change_type, col_slug, revision, changed_by`; all mutation paths now write structured entries (`manual_edit`, `etl_update`, `etl_insert`, `etl_eliminated`, `delete`, `restore`, `bulk_paste`, `rollback`, `insert`); ETL update loop fetches current row once per row for efficient change detection (no-op on unchanged values); new `GET /tools/{tid}/audit?row_tag=&col_slug=&row_tags=&col_slugs=&limit=` endpoint returns structured entries; new `POST /tools/{tid}/rows/{rid}/rollback?col=&entry_id=` restores a cell value to any historical `old_val`; `showCellLog`/`showRowLog`/`showRangeLog` in `grid.js` are now async and fetch from the audit API (no more `__log` text parsing); `showRowLog` opens sidebar instead of modal; tree structure: column → entries for row log, column → row → entries for range log; `↩` rollback button on each audit entry; "Export LOG" button generates downloadable `audit_log.txt`; `__log` column retained (still written) for LOG cell preview backward compatibility; `sidebar.css` adds `.sidebar-log-type`, `.sidebar-log-rollback`, `.sidebar-log-actions` classes


* **Model-first ETL — frontend (steps 5–8)** (2026-05-02): `engine/routes.py` — replaced `EtlSqlBody` with `EtlModelBody`, added `POST /{tool_id}/etl/compile` endpoint (no DB, instant SQL preview); `engine/service.py` — `delete_column` and `update_column` now rewrite via model (load model → mutate alias in select/aggregate columns → recompile → persist), replacing old `sql_parser` calls; `static/engine/js/api.js` — all ETL methods send `{model}` instead of `{sql}`, added `etlCompile(model)`; `static/engine/js/etl_editor.js` — full rewrite from SQL textarea to structured model builder UI: Sources section, Transformations section (SELECT/FILTER/JOIN/AGGREGATE/COMPUTE), Output (final_relation_id) dropdown, Order By, Compiled SQL read-only textarea; schema column click auto-adds source + inserts `alias.col` into focused expr field; debounced compile via `/etl/compile` endpoint; CodeMirror and sql-formatter CDN deps removed
* **Selection-aware grid operations** (2026-05-07): `grid.js` — `_doSaveCell(inputEl, cell, newValue)` refactored from per-field signature to accept cell-shape objects; `_normalizeCellsFromInput(inputEl)` resolves a focused input to `[{row_tag, col_slug}]`; `_getSelectedRowIds()` extracts row IDs from `_ranges`; multi-row soft-delete and hard-delete now delete all selected rows (not just right-clicked one); `_doRemoveOverride(cells)` removes overrides for all selected cells with backward-compat wrapper; `getSelectionForPaste` exposed on public API; `paste.js` — single-value Ctrl+V fills entire selected range; multi-cell paste respects selection bounds when a range is active; `table.html` — context menu label cleanup
* **Task 32 — ETL Editor code folding** (2026-05-07): marked as not needed — syntax validation via inline compile-error banner already covers the use case; item dropped from backlog
* **Group I tasks 47–48 — Project export/import** (2026-05-07): `GET /api/projects/{id}/export` returns the project `.db` as a file download via FastAPI `FileResponse`; `POST /api/projects/import` accepts a `.db` upload, validates SQLite magic bytes and `_project` table presence, copies file to `data/` with unique filename, registers in index via `add_project()`; frontend: export (⬇) button per project row triggers anchor download; "📂 Import from file" button in Open Project modal footer opens file picker and POSTs multipart form
* **UI Rethink — Phase 3 cleanup + Phase 4 floating windows + DnD** (2026-05-07): moved `togglePanelsDropdown`/`closePanelsDropdown` inline JS from `table.html` into `toolbar.js::_initPanelsDropdown()`; `PanelSystem` extended to v3 state (adds `floats: [{id, x, y, w, h}]`, migrates v2 automatically); `moveToFloat(id, x, y)` and `dockPanel(id)` added; `_renderFloats()` creates/destroys `.panel-float` elements in `#panel-float-layer` with titlebar drag and corner resize; HTML5 DnD tab reorder within dock; drag-to-float by dropping tab outside dock (detected via `_dragDropped` flag on `dragend`); float CSS added to `panel_system.css`; `isPanelOpen` now checks both dock and floats
* **Design sync — Conflicts A & B** (2026-05-07): **A** — replaced 40px `.row-num` with 56px `.gutter` sticky column combining row number (`.gutter-num`), REV badge (`.gutter-rev`), and row-level flag dots (`.gutter-flags`/`.gutter-flag-dot`) in `_renderRow` and `_renderGhostRow`; `_attachListeners` selector updated from `td.row-num` → `td.gutter`; `columns.js` header `<th>` updated to `.col-gutter`; `grid.css` gutter block replaces old row-num block. **B** — `.ctx-item` changed from `display:flex` to `display:grid; grid-template-columns: 18px 1fr auto` with new `.ctx-icon` rule; all static `ctx-item` divs in `table.html` updated to 3-span structure (icon | label | shortcut); three inline status strings in `grid.js` wrapped to match the grid layout.
* **UTC timestamp fix** (2026-05-07): `engine/utils.py` — `now_str()` and `format_log_entry()` changed from `datetime.now()` (local time, no tz suffix) to `datetime.now(timezone.utc)` with `%Y-%m-%dT%H:%M:%SZ` / `%Y-%m-%dT%H:%MZ` formats; `engine/service.py` — two inline `datetime.now()` calls in soft-delete and restore flows also fixed; SQLite `DEFAULT (datetime('now'))` was already UTC so no schema migration needed; JS `Date` constructor now parses timestamps unambiguously
* **ETL editor inline compile-error display** (2026-05-07): `templates/engine/etl.html` — added `.etl-compile-error` (hidden banner below textarea, red left-border accent) and `.etl-compiled-sql--error` (red border on textarea) CSS rules; `<div id="etl-compile-error">` element added below the compiled-SQL textarea; `static/engine/js/etl_editor.js` — `_compileAndShow()` rewritten to use dedicated error element: clears error and red border on success, shows banner + red border on `ApiClient.etlCompile` throw; errors are no longer buried inside the read-only textarea as prefixed comments
* **Panel System — K-5/K-6/K-7** (2026-05-07): bottom dock zone + right sidebar resize + proximity snap. `panel_system.js` rewritten as v4 state (adds `bottomDock: {open, height, tabs, activeTab}`; migrates v2/v3 automatically); `_applyRightDock` applies `--sidebar-width` custom property via `style.setProperty`; `_applyBottomDock` mirrors for `--bottom-dock-height`; `_initSidebarResize()` — mousedown on `#sidebar-resize-handle` (K-6): drag left → sidebar wider, max 600px min 200px, transition disabled during drag; `_initBottomResize()` — mousedown on `#bottom-dock-resize-handle` (K-5): drag up → dock taller, max 500px min 80px; `_checkProximity(cx, cy)` (K-7) called on every float mousemove: if cursor within 48px of open dock edge, calls `_showDropHighlight` which overlays `.dock-drop-highlight` at dock bounding rect; on mouseup, `dockPanel(id, target)` snaps float into that dock; snap only fires when dock already has tabs; `dockPanel(id, target)` now accepts `'right'` (default) or `'bottom'`; `showPanel(id, {dock:'bottom'})` opens into bottom dock; `closeBottomDock()` added; `_initTabBarEvents(bar, dockName)` generalized to serve both docks; bottom dock header shown only for single-panel dock; `table.html` — `#sidebar-resize-handle` added as first child of `#sidebar-panel`; `#bottom-dock` added after `.tool-body`; `sidebar.css` — `.sidebar-panel` width changed to `var(--sidebar-width, 320px)`, `position: relative` added, `.sidebar-resize-handle` CSS added; `panel_system.css` — `.bottom-dock`, `.bottom-dock-resize-handle`, `.bottom-dock-header/body`, `.dock-drop-highlight` added
* **Panel System — Phase 3** (2026-05-07): `static/engine/js/panel_system.js` — new IIFE: panel registry (`register({id, title, icon, onActivate})`), right-dock layout state persisted to `localStorage` key `'instrumentManager.layout.v2'`, `showPanel(id, {silent})` / `hidePanel(id)` / `togglePanel(id)` / `closeAll()` with `_applyLayout()` rendering `.panel-tab-bar` inside `#sidebar-panel`; tab bar shown only when 2+ panels open; `silent:true` flag prevents `onActivate` from firing when callers fill body themselves (prevents loop with `flags.js`/`grid.js`); `static/engine/css/panel_system.css` — tab bar styles (`.panel-tab-bar`, `.panel-tab`, `.panel-tab.active`, `.panel-tab-close`); `static/engine/js/sidebar.js` rewritten as thin adapter: `open(title)` → `PanelSystem.showPanel(id, {silent:true})`, `close()` → `closeAll()`, `toggle()` → `togglePanel('info')`, `setContent/setTitle` write DOM directly; `table.html` registers 3 panels (`info`, `log`, `flags`) in `DOMContentLoaded` with appropriate `onActivate` callbacks; Panels dropdown updated to call `PanelSystem.togglePanel('flags'/'info')` instead of direct `FlagsManager.show()`/`SidebarManager.toggle()`; script load order updated: `panel_system.js` before `sidebar.js`
* **UI redesign — Phase 4+5 completion: panel content + context menu** (2026-05-07): `static/engine/css/panels.css` — new file: `.panel-placeholder`, `.panel-meta-grid`/`.panel-meta-row`/`.panel-meta-key`/`.panel-meta-val`/`.panel-meta-etl` for Info panel, `.panel-tl-*` timeline for Log panel; `templates/engine/table.html` — `panels.css` link added; info panel `onActivate` now calls `_refreshInfo(body)` which shows col name/type/value for single-cell focus or row/col/cell-count + numeric stats for range; log panel `onActivate` calls `_refreshLog(body, rowTag)` which fetches `ApiClient.getAudit({rowTag})` and renders `.panel-tl-item` timeline entries; two document-level event listeners (`focusin` on `.cell-input`, `mouseup` deferred via `setTimeout`) keep panels live as user navigates; `_getPanelContainer(id)` helper resolves float vs dock body; `_logLastRowTag` avoids redundant API calls on same row; `static/engine/css/grid.css` — `.ctx-section` header label style, second `.ctx-item` rule adds `display:flex`/`justify-content:space-between` for shortcut hints, `.ctx-shortcut` right-aligned muted text; context menu delete item gets `Del` shortcut hint
* **UI redesign — Phase 1+2: design tokens, app shell, i18n** (2026-05-07): `static/css/main.css` — full rewrite: oklch design tokens (`--bg`, `--surface`, `--ink`, `--accent`, etc.), light/dark theme (`[data-theme]`), 4 accent variants (`[data-accent]`), density (`[data-density]`), CSS grid app shell (`.app` → `grid-template-areas: "topbar topbar" "side main"`), new left sidebar (`.side-item`, `.si-icon`, `.si-label`, `.si-stale`), topbar (`.crumbs`, `.tool-pill`, `.status-chip`, `.chip-rev`), popovers, segmented controls, settings modal CSS; backward-compat aliases (`--color-bg: var(--bg)`) keep engine CSS unmodified; `templates/base.html` — Google Fonts (Inter + JetBrains Mono), `[data-theme/accent/density]` on `<html>`, new `.app` grid structure, redesigned topbar + sidebar, loads `i18n.js` + `app_shell.js` globally; `static/js/main.js` — sidebar toggle now uses `data-sidebar` attribute on `#app` (CSS grid approach); sidebar items use new `.side-item/.si-icon/.si-label/.si-stale` class names; `static/engine/js/i18n.js` — new IIFE with EN/IT string tables, `localStorage` persistence; `static/engine/js/app_shell.js` — new IIFE: theme/accent/density management (persisted to `im.prefs`), settings modal with Appearance+Language tabs rendered dynamically, tool-pill popover (name+icon inline edit → `ApiClient.updateToolSettings`), REV chip popover (revision inline edit → `ApiClient.updateToolSettings`), popovers body-appended with `position:fixed` + `getBoundingClientRect` to avoid overflow clipping, auto-init via `DOMContentLoaded`; `templates/engine/table.html` — `{% block topbar_crumbs %}` override with tool-pill + crumb separator, `{% block topbar_actions %}` override with status chip + export button, secondary toolbar redesigned: back+search on left, ETL reload+link+density-segmented+Panels-dropdown on right; old modal-settings removed; `static/engine/js/toolbar.js` — `_updateToolUI()` updated to populate `#topbar-tool-pill .pill-name/.tool-icon` and `#chip-rev-btn`, uses new `.si-icon/.si-label` sidebar class names
* **K-1 + K-2: Selection-aware Info and History panels** (2026-05-07): `templates/engine/table.html` — full rewrite of panel JS; `_resolveSelection(ranges)` helper computes bounding box + `isSingle` flag; Info panel: single cell shows tag header + ROW/COLUMN/VALUE/REV/LINEAGE meta grid + async CHANGES timeline (row-level audit fetch); range shows "= NR × MC" header + ROWS/#range/TAGS/COLUMNS/CELLS(filled+empty)/DISTINCT-VALUES-count/LINEAGE meta grid + optional NUMERIC STATS (sum/avg/min/max/n) + DISTINCT VALUES list with short row-tag pills; History panel: single cell (`{type:'cell', rowTag, colSlug}`) fetches col-specific audit and renders flat timeline; range (`{type:'range', rowTags[], colSlugs[]}`) fetches combined audit and groups by col_slug → row_tag with nested timeline; `_selFromRanges()` helper resolves ranges to the right selection shape; `_renderTimeline()` shared helper; `_logLastKey` dedup key; `static/engine/css/panels.css` — complete rewrite: `.panel-cell-header`/`.panel-cell-tag-label`/`.panel-cell-tag-val`, `.panel-range-header`, `.panel-divider`, `.panel-section-label`, `.panel-meta-key` (uppercase 10px, no white-space:nowrap, 90px fixed col), `.panel-meta-muted`, `.panel-lineage-etl`/`.panel-lineage-manual`/`.panel-lineage-override`, `.panel-dv-list`/`.panel-dv-item`/`.panel-dv-value`/`.panel-dv-tag`, `.panel-hist-group`/`.panel-hist-group-col`/`.panel-hist-subgroup`/`.panel-hist-subgroup-tag`; panel title renamed "Info" → "Cell info".
* **K-4: Notes + SQL Editor as panels** (2026-05-07): `sql_editor.js` — removed inline toggle/state, added `_sqlDraft`/`_resultsHtml` module state for persistence across reopen, `renderInto(body)` creates fresh UI in given panel body, `toggle()` delegates to `PanelSystem.togglePanel('sql')`, Ctrl+Enter guard uses `PanelSystem.isPanelOpen('sql')`, strings switched to English; `templates/engine/table.html` — `.tool-note-area` and `#sql-editor-panel` removed from `.tool-main`, hidden `<textarea id="tool-note">` added as shared note state (ToolbarManager reads/writes it), `notes` and `sql` panels registered in DOMContentLoaded, Panels dropdown updated (Notes added, SQL changed to `PanelSystem.togglePanel`); `panels.css` — `.panel-notes-input` added; `sql_editor.css` — `.sql-editor-panel` and `.sql-editor-header` removed (no longer in DOM).
* **ETL Canvas — per-tool pipeline visualization** (2026-05-07): new page `GET /project/{id}/canvas/{tool_id}` renders a tool's `etl_model` as a draggable node graph; source nodes (📊 table, 📝 CTE, 🔢 generate_series) and transformation nodes (📋 select, 🔍 filter, 🔗 join, ∑ aggregate, ƒ compute) with type-colored left borders; SVG bezier edges from `inputs[]` arrays; BFS topological auto-layout, pan/zoom/drag (positions in localStorage per tool); final-output node highlighted with green badge; ▶ Run ETL button calls `POST /api/tools/{id}/etl/run`; ✏ Edit ETL and ⊞ Grid links in topbar; `etl_design.js` updated so left-click on tool card → canvas, Shift+click → grid, Ctrl+click → ETL editor; `static/engine/js/etl_canvas.js` + `.md` + `static/engine/css/etl_canvas.css` + `templates/etl_canvas.html` created
* **Group M Phase 1 — Visual ETL Canvas editor shell** (2026-05-08): Interactive canvas view added to the existing ETL editor page (`/tool/{pid}/{tid}/etl`); Canvas / Code toggle in the internal topbar syncs the shared model via `EtlEditor.getModel()` / `EtlEditor.loadModel()` on every switch; canvas renders ETL model sources and transformations as draggable node cards with SVG bezier edges, pan/zoom/drag (positions in localStorage); `+` port button on each node → popup → `addNode(type)` appends a new transformation to the model, auto-positions it, and opens the side panel; side panel slides in from the right on node click (Phase 1 shows raw JSON; Phase 2 will add DSL formula bar); Destination node reads tool columns via `GET /api/tools/{id}/columns` and shows all target columns; `etl_editor.js` extended with `getModel()` / `loadModel(m)` bridge methods; new `static/engine/js/etl_canvas_editor.js` (347 lines) + companion `.md`
* **Group M — ETL Design canvas** (2026-05-07): project-level DAG view at `GET /project/{id}/etl-design`; tools rendered as draggable node cards (color-coded border: green=ok, amber=stale, gray=no ETL) with SVG cubic-bezier edges; pan (drag background), zoom (wheel, 0.3×–3×), node drag (positions persisted in localStorage per project); Run Stale / Run All buttons with topological sort; Refresh updates staleness badges without full DOM rebuild; nav entry in sidebar bottom (hidden until project activates); API: `GET /api/projects/{id}/etl-graph`, `POST etl-run-stale`, `POST etl-run-all` in `core/routes.py`
* **K-3: Rename LOG → History + i18n wiring + density fix** (2026-05-07): `panel_system` panel id `'log'` renamed to `'history'`, title `'LOG'` → `'History'`; `sidebar.js` `_ID_MAP` updated (`'History'`/`'LOG'` → `'history'`); all `SidebarManager.open('LOG')` → `'History'`, title strings `LOG — …` → `History — …`, "Export LOG" → "Export history" in `grid.js`; context menu items renamed "View row/cell/selection history"; `.panel-tl-col` in `panels.css` now renders as an accent-colored tag pill (border-radius, mono font, faint bg); `i18n.js` — added `applyLocale()` (walks `[data-i18n]` / `[data-i18n-placeholder]` / `[data-i18n-title]` and swaps text/placeholder/title), `setLang()` now calls `applyLocale()`; `app_shell.js` `init()` calls `applyLocale()` on load; `base.html` sidebar labels and `table.html` search placeholder, density buttons, notes label, context menu items now carry `data-i18n` / `data-i18n-placeholder` attributes; IT/EN string table updated (`ctx.row_log` → "View row history" / "Vedi cronologia riga" etc.); M-2 density fix: `grid.css` `.cell-input` and `.cell-actions` now use `var(--cell-pad-y)`, `var(--cell-pad-x)`, `var(--row-h)` so grid row height responds to the `[data-density]` token set by `AppShell.setDensity()`

* **Group M Phase 3 — Remaining Node Types** (2026-05-08): `etl_canvas_panel.js` extended with four new HTML builders: `_aggregateHtml` (group-by list `#ecv-gb` + aggregations list `#ecv-agg`), `_computeColHtml` (single alias + formula textarea), `_genSeriesHtml` (alias + integer start + end_expr formula bar), `_cteHtml` (name + alias + raw SQL textarea); new bind functions: `_bindGbList` (delegated blur/click on `#ecv-gb`, group-by exprs parsed via DSL), `_bindAggColList` (delegated blur/click on `#ecv-agg`, aggregation alias+expr), inline bindings for compute/generate_series/cte in `_bindPanel`; `open()` now dispatches `table`→read-only, `cte`/`generate_series`→editable source panels, all transformation types to specific builders; fallback to raw JSON removed for known types; `etl_canvas_editor.js` updated: `_tLabel` adds `compute_column` key (was `compute`), `_tDetail` returns detail for `aggregate` and `compute_column`, `_nodeIcon` maps `compute_column→ƒ`, `addNode` initializes `group_by`/`aggregations` for aggregate and `column` object for compute_column; `etl.html` popup adds ∑ Aggregate and ƒ Compute buttons; CSS `.ecv-node--compute_column` and `.ecv-gb-row` added.
* **Group M — Canvas: Add Source + Edge Data Preview** (2026-05-08): two new canvas features added. (1) **Add Source**: floating "+ Source" button in the top-left of the canvas opens a popup that fetches `GET /api/tools/project/{PROJECT_ID}` (`ApiClient.listProjectTools()`) and lists all project tools as table sources plus CTE and generate_series options; `addSource(type, name, alias)` appends the new source to `_model.sources`, auto-positions it in the left column (130px gap), and opens the edit panel for non-table types; `api.js` extended with `listProjectTools()`; `#ecv-src-popup` + `#ecv-toolbar` HTML + CSS added to `etl.html`. (2) **Edge data preview**: clicking a connection line calls `ApiClient.etlPreview` with a **partial model** (`final_relation_id = fromNode`) to run only the pipeline up to that stage; result shown in a floating `#ecv-preview-panel` (480×320px max, scrollable, up to 50 rows); hit-area paths (`.ecv-edge-hit`, `stroke-width: 14`, transparent, `pointer-events: stroke`) added to SVG alongside the visible paths; `#ecv-edges-svg` no longer has `pointer-events: none`; pan handler guards against edge-click via `classList.contains('ecv-edge-hit')`; new `etl_canvas_preview.js` IIFE (`EtlCanvasPreview`) handles `show(data, x, y, fromId)`, `showError()`, `close()`; loaded before `etl_canvas_editor.js`.
* **Group M — Visual ETL Canvas (all phases complete)** (2026-05-08): interactive canvas view added to the ETL editor page alongside the existing code view (same URL); Canvas/Code toggle syncs the shared `etl_model` on every switch; pan/zoom/drag DAG with SVG bezier edges; `+` port popup adds transforms; `+ Source` floating button adds new sources; side panel with type-specific forms (Select, Filter, Join, Aggregate, Compute Column, CTE, generate_series, Destination); restricted DSL formula bar (`etl_dsl.js`) with recursive-descent parser + serializer; edge-click partial preview panel; all node types implemented; no raw SQL in model — canvas compiles to AST only. See `_context/VISUAL_ETL_CANVAS.md` for design rationale.

* **Stateless server + local file management (Plan D-S1–D-S8)** (2026-05-08): removed `data/projects.db` and `engine/project_index.py`; server is now a pure file-operation layer — project identity is the full `.db` path carried as `?db=...` on every API call; `engine/project_db.py::get_project_conn` reads `?db=` directly instead of looking up an integer id; all `engine/routes.py` endpoints migrated from `{project_id}` path param to `?db=` query param; `core/routes.py` rewritten: `POST /api/project/new`, `POST /api/project/open`, `GET/DELETE /api/project`, `POST /api/project/backup`, `GET /api/fs/browse`, `GET /api/fs/cwd`; `main.py` — removed `init_index()`, page routes now pass `db` string to templates; `static/js/main.js` rewritten: localStorage recents (`im_recent_projects`), `_openProjectFromPath`, filesystem browser IIFE, backup helpers; `static/engine/js/api.js` — all calls use `db=${encodeURIComponent(DB_PATH)}`; `panel_system.js` and `flags.js` storage keys use djb2 hash of `DB_PATH`; `etl_canvas.js`, `etl_canvas_editor.js`, `etl_design.js`, `etl_editor.js`, `toolbar.js`, `app_shell.js` — `PROJECT_ID` references replaced with `DB_PATH`; all templates updated: `table.html`, `etl.html`, `etl_canvas.html`, `etl_design.html`; `base.html` — topbar settings button removed, side-bottom gains New/Open project buttons + filesystem browser modal; `index.html` welcome card body replaced with recent projects list; `static/css/main.css` — styles added for recent items, filesystem browser; settings modal gains Backup tab (on-open toggle, timer interval, keep count, subfolder name) saved into `im.prefs.backup`

* **Group M Phase 2 — Restricted DSL Formula Bar** (2026-05-08): new `etl_dsl.js` IIFE (`EtlDsl`) implements a recursive-descent tokenizer + parser and serializer for the restricted expression DSL; supported AST types: `column_ref` (`alias.col` or bare `col`), `literal` (string/number/null/bool), `function` (any UPPER-mapped call), `binary_op` (=, !=, <, >, <=, >=, +, -, *, /), `logical` (and/or/not), `is_not_null`/`is_null`, `case` (flat alternating pairs + optional else); special DSL syntax: `case(cond1,val1,...,else)`, `not_null(expr)`, `and(...)`, `or(...)`; public API: `parse(src)` throws on error, `tryParse(src)` returns `{ast,error}`, `serialize(ast)` is the inverse; new `etl_canvas_panel.js` IIFE (`EtlCanvasPanel`) renders type-specific side panels: Source (read-only name/alias/type), Select (column list with alias + formula bars), Filter (mode select + condition formula bar), Join (join-type select + right-source dropdown + condition formula bar + projection column list), Destination (tool column list); formula bar validates on blur via `EtlDsl.tryParse`, shows inline error, and writes AST back into `_model` via in-place mutation + `onChange()` callback; column add/remove rebuilds only the list rows in `#ecv-cols` using delegated listeners; `etl_canvas_editor.js` updated: `_openPanel` delegates to `EtlCanvasPanel.open(...)`, `_getSourceList()` builds dropdown options, `_onModelChange()` calls `_render()`; `etl.html` updated: `etl_dsl.js` and `etl_canvas_panel.js` added to script load order, CSS added for all panel form elements (`ecv-formula-bar`, `ecv-col-list`, `ecv-col-row`, `ecv-col-alias`, `ecv-col-formula`, `ecv-panel-select`, `ecv-info-row`, etc.)

* **Group N — Context menu cleanup** (2026-05-08): removed "View row history" item (`data-action="log"`) from the row right-click context menu — already accessible via the History panel; renamed "View cell history" → "History" (`ctx.cell_log` key updated in both EN and IT locales); removed `action === "log"` handler branch from `grid.js`; separator layout unchanged.

* **Item 43 — Virtual scrolling + `__position` index** (2026-05-08): `grid.js` now renders only the visible window (~2×OVERSCAN=10 rows above/below viewport) between two `<tr class="vs-spacer">` height-spacer rows; single `render()` path called on scroll (RAF-throttled), filter/search, data mutations; `_ranges` preserved across scroll repaints (no longer cleared in `render()`), `_updateRangeHighlight()` re-applies CSS after each paint; `_moveFocus()` redesigned to use absolute `data-row-idx` from `<td>` and `_scrollRowIntoView()` so keyboard nav crosses virtual window boundaries; `_createFromGhost` sets `container.scrollTop` before `render()` so new rows always land in viewport; density change re-triggers `render()` via `MutationObserver` on `data-density`; column copy reads `_filteredRows` directly, unaffected by virtual window; `project_db.py::create_tool_table` adds `CREATE INDEX IF NOT EXISTS idx_{slug}_pos` on `__position`; `_migrate_project_db` auto-creates the index on all existing tool tables at server start; see D11 in `DECISIONS.md`.

* **Group J — Schema versioning + compatibility** (2026-05-08): `PRAGMA user_version` stores schema version; `SCHEMA_VERSION = 1` constant in `engine/project_db.py` is the server's current version; `_migrate_project_db` replaced by versioned runner `_run_migrations` (one transaction per version step with automatic rollback on failure); all pre-existing ad-hoc checks absorbed into `_migrate_to_v1`; new projects created with `user_version = SCHEMA_VERSION`; safety backup to `data/backups/<stem>_pre_migration_v<N>.db` taken before any migration (idempotent — skipped if exists); newer-DB-than-server: `get_project_conn` returns HTTP 403 on non-GET requests; `GET /api/project` returns `schema_warning` string when DB version exceeds server version; bump-check rule added to `engine/project_db.py.md` Decisions section and to memory; architectural decision recorded as D12 in `_context/DECISIONS.md`

* **Architecture audit + cleanup** (2026-05-08): full audit of all JS modules, backend routes, and templates for legacy leftovers, dead code, duplicated responsibilities, and architectural drift. Findings: overall architecture is sound — Panel System v3 is the single source of truth, no duplicate state systems, ETL canvas correctly integrated. Six targeted fixes applied: (1) deleted orphaned `static/js/main (1).js` (old pre-stateless-server copy of main.js); (2) fixed `etl_canvas.js:349` — `_projectId` (undefined after D-S1 migration) replaced with `GET /api/tools/project?db=...` raw fetch; (3) removed dead `const showToast = Utils.showToast` alias from `etl.html` inline script (declared but never called); (4) removed `project_id: Optional[int] = None` from `ToolResponse`, `TemplateCreate`, `TemplateResponse` Pydantic models (never populated after stateless migration, no frontend consumer); (5) renamed `row_id` → `trash_id` in `restore_row` route for semantic correctness (route operated on `_trash.id`, not main-table `__id`); (6) updated `etl_canvas_editor.js.md` to document ApiClient dependency. Two deferred items: `etl_editor.js` private-function exposure (safe, needs event delegation refactor) and History/Flags `onActivate` migration (needed only if dock mobility is required). See audit plan at `_context/DECISIONS.md` for the LIMIT-1 and QUAL-1 items.

* **History Panel bug fix** (2026-05-08): fixed 4 compounding bugs causing "No history for this cell" and broken selection overlay after rollback. (1) `refreshRowDOM` now calls `_attachListeners()` + `_updateRangeHighlight()` after `outerHTML` replacement so new DOM elements get event listeners and the blue selection overlay is restored. (2) `_rollbackCell` refactored to use `refreshRowDOM` instead of duplicating DOM replacement code. (3) `panel_system.js` floating panel path now respects `{silent:true}` so `onActivate` is not fired when `SidebarManager.open('History')` is called from `showCellLog`. (4) `showCellLog`/`showRowLog` (old rendering path) dispatch `grid:historyRendered` event; `table.html` listens and resets `_logLastKey = null` to prevent the new-path `_refreshLog` cache from going stale after old-path content is set.
